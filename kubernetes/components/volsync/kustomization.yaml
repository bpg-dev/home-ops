---
# yaml-language-server: $schema=https://json.schemastore.org/kustomization
apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - ./externalsecret.yaml
  # Temporarily excluded restore PVC to avoid conflicts with app PVCs
  # The restore PVC (pvc.yaml) is only needed for restore scenarios and can be created manually when needed
  # - ./pvc.yaml
  - ./replicationdestination.yaml
  - ./replicationsource.yaml
